// Backup of Hero.jsx created automatically

import { useEffect, useRef } from "react";
import gsap from "gsap";
import { motion, useScroll, useTransform } from "framer-motion";
import spidermanFace from "../assets/images/spiderman-face.jpg";
import sagarImage from "../assets/images/sagar.jpg";

const HeroBackup = () => {
  return (
    <section className="relative h-screen flex items-center justify-center px-6">
      <div className="text-white">Hero backup - original content saved.</div>
    </section>
  );
};

export default HeroBackup;
